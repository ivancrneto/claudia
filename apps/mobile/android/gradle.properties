# Non-secret build settings only. Signing credentials come from the environment in CI
# (see app/build.gradle and docs/ANDROID_RELEASE.md) — never put keystore passwords here.
org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official
